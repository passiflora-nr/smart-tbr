---
import { buildBooksHref, hasActiveFilters, serializeBookFilters, type BookFilters } from "@/lib/book-filters";

interface Props {
  filters: BookFilters;
  tropeVocabulary: string[];
}

const { filters, tropeVocabulary } = Astro.props;
const selectedTropes = new Set(filters.tropes);
const selectedTropeCount = filters.tropes.length;
const tropeSummaryLabel =
  selectedTropeCount === 0
    ? "Tropes"
    : selectedTropeCount === 1
      ? "Tropes · 1 selected"
      : `Tropes · ${selectedTropeCount} selected`;
const clearSearchHref = buildBooksHref(serializeBookFilters({ q: "", tropes: filters.tropes }));
const filtersActive = hasActiveFilters(filters);
---

<form method="GET" action="/books" class="border-border bg-card mb-6 rounded-lg border p-4 shadow-sm">
  <div class="flex flex-col gap-3 sm:flex-row sm:items-end">
    <div class="min-w-0 flex-1">
      <label for="book-filter-q" class="text-foreground mb-1 block text-sm">Search title or author</label>
      <div class="relative">
        <input
          type="text"
          id="book-filter-q"
          name="q"
          value={filters.q}
          maxlength={300}
          placeholder="Search…"
          class="border-input bg-card text-foreground placeholder:text-muted-foreground w-full rounded-lg border py-2 pr-9 pl-3 text-sm"
        />
        {
          filters.q.length > 0 ? (
            <a
              href={clearSearchHref}
              aria-label="Clear search"
              class="text-muted-foreground hover:bg-background hover:text-foreground absolute top-1/2 right-2 inline-flex -translate-y-1/2 rounded-md p-1 transition-colors"
            >
              <svg
                xmlns="http://www.w3.org/2000/svg"
                width="16"
                height="16"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
                class="size-4"
                aria-hidden="true"
              >
                <path d="M18 6 6 18" />
                <path d="m6 6 12 12" />
              </svg>
            </a>
          ) : null
        }
      </div>
    </div>

    {
      tropeVocabulary.length > 0 ? (
        <div class="relative shrink-0">
          <p
            class="text-foreground mb-1 block text-sm sm:invisible sm:mb-0 sm:h-0 sm:overflow-hidden"
            aria-hidden="true"
          >
            Tropes
          </p>
          <details class="group">
            <summary class="border-border bg-card text-foreground hover:bg-background flex cursor-pointer list-none items-center gap-2 rounded-lg border px-3 py-2 text-sm transition-colors [&::-webkit-details-marker]:hidden">
              <svg
                xmlns="http://www.w3.org/2000/svg"
                width="16"
                height="16"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
                class="text-muted-foreground size-4 shrink-0 transition-transform group-open:rotate-180"
                aria-hidden="true"
              >
                <path d="m6 9 6 6 6-6" />
              </svg>
              {tropeSummaryLabel}
            </summary>
            <div class="border-border bg-card absolute left-0 z-10 mt-1 max-h-64 w-72 overflow-y-auto rounded-lg border p-3 shadow-sm sm:right-0 sm:left-auto">
              <fieldset>
                <legend class="sr-only">Filter by tropes</legend>
                <ul class="space-y-2">
                  {tropeVocabulary.map((trope) => (
                    <li>
                      <label class="text-foreground flex cursor-pointer items-start gap-2 text-sm">
                        <input
                          type="checkbox"
                          name="trope"
                          value={trope}
                          checked={selectedTropes.has(trope)}
                          class="border-input bg-card text-primary mt-0.5 shrink-0 rounded"
                        />
                        <span class="min-w-0 break-words">{trope}</span>
                      </label>
                    </li>
                  ))}
                </ul>
              </fieldset>
            </div>
          </details>
        </div>
      ) : null
    }

    <div class="flex shrink-0 flex-wrap items-center gap-3">
      <button
        type="submit"
        class="border-border bg-card text-foreground hover:bg-background rounded-lg border px-4 py-2 text-sm transition-colors"
      >
        Apply filters
      </button>
      {
        filtersActive ? (
          <a
            href="/books"
            class:list={[
              "rounded-lg border px-4 py-2 text-sm transition-colors",
              "border-border bg-card text-primary hover:bg-background hover:text-primary-hover",
            ]}
          >
            Clear filters
          </a>
        ) : (
          <span
            class:list={[
              "rounded-lg border px-4 py-2 text-sm transition-colors",
              "border-border bg-background text-muted-foreground cursor-not-allowed",
            ]}
            aria-disabled="true"
          >
            Clear filters
          </span>
        )
      }
    </div>
  </div>
</form>
